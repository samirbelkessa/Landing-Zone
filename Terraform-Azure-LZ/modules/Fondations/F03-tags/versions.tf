# =============================================================================
# Module: tags (F03)
# Description: Standardized tags for Azure Landing Zone CAF
# =============================================================================

terraform {
  required_version = ">= 1.5.0"

  required_providers {
    # No provider required - this is a pure Terraform module
    # that only uses locals and outputs
  }
}
